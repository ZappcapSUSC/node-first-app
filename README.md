# Notas:

Node app

```
Options:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     La base para el calculo de la tabla de multiplicar
                                                            [número] [requerido]
  -l, --listar   Muestra la tabla resultante         [booleano] [defecto: false]
  -h, --hasta                                             [número] [defecto: 10]
```
